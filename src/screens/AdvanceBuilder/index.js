import React from 'react';
import {View, Text, StyleSheet} from 'react-native';

const AdvanceBuilder = ({navigation}) => {
  return (
    <View>
      <Text>Advance Builder</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default AdvanceBuilder;
